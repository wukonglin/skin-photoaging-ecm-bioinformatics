# requirements.txt -- pip fallback for the PYTHON side of this project.
# Prefer the conda environment (environment.yml -> env "skin-ecm-bioinformatics"); use this
# only if you must install into a plain venv:
#     python -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt
#
# (The R/Bioconductor stack is NOT covered here -- see environment_R.yml.)

# --- local file / context parsing ---
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
lxml>=5.0
beautifulsoup4>=4.12
html5lib>=1.1
requests>=2.31
PyYAML>=6.0
tqdm>=4.66

# --- core scientific stack ---
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14

# --- plotting ---
matplotlib>=3.7
seaborn>=0.13

# --- single-cell (optional Python route) ---
scanpy>=1.10
anndata>=0.10
leidenalg>=0.10
python-igraph>=0.11

# --- enrichment / pathway (optional Python route) ---
gseapy>=1.1
geoparse>=2.0
decoupler>=1.6
# pydeseq2>=0.4   # optional: bulk DE in Python

# --- notebooks ---
jupyterlab>=4.0
ipykernel>=6.29
